import { Section } from '@/layout/Section';
import RegistrationForm from '@/registrationForm/RegistrationForm';

const Form = () => (
  <Section>
    <RegistrationForm />
  </Section>
);

export default Form;
